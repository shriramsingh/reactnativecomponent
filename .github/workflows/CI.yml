name : Android build
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      uses: actions/checkout@v2
      -name: use node js v12
      uses: actions/setup-node@v1
      with: 
        node-version: 12.x
      run: nmp i